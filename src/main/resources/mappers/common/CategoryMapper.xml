<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="wang.redbean.blog.common.mapper.CategoryMapper">

    <sql id="Base_Column_List">
         category_id AS categoryId,parent_id AS parentId,category_name AS categoryName,
         category_type AS categoryType,category_status AS categoryStatus,
         create_time AS createTime,update_time  AS updateTime
    </sql>

    <select id="selectByName" resultType="wang.redbean.blog.common.model.Category" parameterType="java.lang.String">
        select
         <include refid="Base_Column_List"/>
        from common_category
        where category_name = #{categoryName}
    </select>

    <select id="selectCategoryChildrenByParentId" resultType="wang.redbean.blog.common.model.Category" parameterType="java.lang.Integer">
        select
        <include refid="Base_Column_List"/>
        from common_category
        where parent_id = #{parentId,jdbcType=INTEGER}
    </select>

</mapper>